import 'package:flutter/material.dart';

class AppTheme {
  static const Color primaryColor = Color(0xFF007AFF);
  static const Color secondaryColor = Color(0xFF007AFF);
  static const Color tertiaryColor = Color(0xFF007AFF);
  static const Color quaternaryColor = Color(0xFF007AFF);
  static const Color quinaryColor = Color(0xFF007AFF);
  static const Color senaryColor = Color(0xFF007AFF);
  static const Color septenaryColor = Color(0xFF007AFF);
  static const Color octonaryColor = Color(0xFF007AFF);
  static const Color nonaryColor = Color(0xFF007AFF);

  ThemeData getTheme() => ThemeData(
        brightness: Brightness.dark,
      );
}
